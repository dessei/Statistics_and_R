#packages only need to be installed once
#example:
# install.packages("ggplot2") #was run in console

library(ggplot2) #to use it in the actual code

#the real fun is now looking through the documentation to search for what
#you're actually looking for in the current context of the objective
